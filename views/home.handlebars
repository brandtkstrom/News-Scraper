<div class="container">
    <div class="grid">

        <div class="row justify-content-center">
            <h1 class="display-3">Under Construction</h1>
        </div>

        <div class="row">
            {{#each headlines}}
                <p>{{title}}</p>
            {{/each}}
        </div>

    </div>
</div>